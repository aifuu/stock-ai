name: LEGACY DISABLED - 5分足 自動決済監視

on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'この旧5分足決済ワークフローは停止済みです。統合版AI Stock Scan - Directional TOP1 5分足 Paper Tradeが決済と再エントリーを一元管理します。'
