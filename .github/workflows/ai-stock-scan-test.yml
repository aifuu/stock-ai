name: AI Stock Scan TEST - loop guard validation (fix/scan-loop-guard, DO NOT MERGE)

# このワークフローは fix/scan-loop-guard ブランチの検証専用。
# - schedule無し(workflow_dispatchのみ)
# - concurrency groupを本番(stock-ai-paper-trading)と分離
# - Discord通知を無効化(DISCORD_WEBHOOKを渡さない→discord_progressが自動でno-op)
# - state commit/pushを無効化(本番のpaper trading stateを一切変更しない)
# 検証完了後はこのファイルとブランチを削除する。

on:
  workflow_dispatch:
    inputs:
      force_date:
        description: 'テスト用: 営業日判定に使うJST日付を上書き (YYYY-MM-DD)。空なら実際の日付を使用。'
        required: false
        type: string
      hard_wall_minutes:
        description: 'テスト用: ループを強制終了させるまでの分数上限(本番は320分固定)。'
        required: false
        default: '8'
        type: string
      force_am_fail:
        description: 'テスト用: trueだとAMセッションループを営業日ガード通過直後にexit 1で失敗させる(if:!cancelled()でPMが起動するかの検証用)。'
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: stock-ai-paper-trading-test
  cancel-in-progress: false

jobs:
  session_am:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SESSION: AM
    steps:
      - name: Checkout latest main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas numpy requests scikit-learn joblib jpholiday

      - name: Apply Nikkei 225 stock universe
        run: |
          python apply_expanded_universe.py
          python - <<'PY'
          import ast
          from pathlib import Path
          tree=ast.parse(Path('daily_directional_top1.py').read_text(encoding='utf-8'))
          vals=[]
          for node in tree.body:
              if isinstance(node,ast.Assign):
                  for target in node.targets:
                      if isinstance(target,ast.Name) and target.id=='TICKERS': vals=ast.literal_eval(node.value)
          print(f'✅ Runtime universe = {len(vals)}銘柄')
          if len(vals) != 225: raise SystemExit('❌ 日経225ユニバースが225銘柄ではありません')
          PY

      - name: Syntax check
        run: |
          python -m py_compile nikkei225_universe.py apply_expanded_universe.py intraday_scheduler.py run_profit_loop.py paper_fast_entrypoint.py paper_profit_entrypoint.py paper_risk_policy.py daily_model_retrain.py walk_forward.py adversarial_strategy_validator.py adversarial_oos_diagnostic.py multi_oos_profit_validator.py build_verified_profit_policy.py daily_directional_top1.py profit_top10_paper.py profit_top10_monthly_performance.py intraday_directional_top1_cycle.py

      - name: Verify Nikkei 225 tickers
        run: |
          python - <<'PY'
          import ast
          from pathlib import Path
          files=['daily_directional_top1.py','walk_forward.py','stock_scan.py']
          counts=[]
          for f in files:
              tree=ast.parse(Path(f).read_text(encoding='utf-8')); vals=[]
              for node in tree.body:
                  if isinstance(node,ast.Assign):
                      for target in node.targets:
                          if isinstance(target,ast.Name) and target.id=='TICKERS': vals=ast.literal_eval(node.value)
              if len(vals)!=225 or len(vals)!=len(set(vals)): raise SystemExit(f'❌ {f}: {len(vals)}銘柄/重複あり')
              counts.append(len(vals)); print(f'✅ {f}: {len(vals)}銘柄・重複なし')
          if len(set(counts))!=1: raise SystemExit(f'❌ 3ファイルの銘柄数不一致: {counts}')
          PY

      - name: Morning premarket phase marker
        run: python intraday_scheduler.py --phase premarket

      - name: Verify paper policy mode
        run: |
          test -s strategy_policy.json
          python - <<'PY'
          import json
          p=json.load(open('strategy_policy.json',encoding='utf-8'))
          status=str(p.get('status','')).upper()
          if status not in ('APPROVED','PENDING'):
              raise SystemExit(f'❌ 不正なpolicy status: {status}')
          print(f'✅ paper policy mode = {status}')
          if status == 'PENDING': print('⚠️ PENDINGでも紙取引は継続。実注文には使用しません。')
          PY

      - name: AM session loop [TEST] (休場日ガード/キャンセル応答性の検証用)
        env:
          AI_POLICY_SIGNING_SECRET: ${{ secrets.AI_POLICY_SIGNING_SECRET }}
          AI_INITIAL_CAPITAL: '1000000'
          INTRADAY_FEE_RATE: '0.00055'
          AI_DAILY_STOP_LOSS: '0.015'
          AI_MAX_DRAWDOWN: '0.30'
          AI_MAX_POSITIONS: '10'
          MAX_TRADES_PER_DAY: '30'
          MAX_TRADES_PER_TICKER_PER_DAY: '10'
          SAME_TICKER_COOLDOWN_MINUTES: '30'
          PAPER_TRADE_MODE: '1'
          TEST_FORCE_DATE: ${{ inputs.force_date }}
          TEST_HARD_WALL_MIN: ${{ inputs.hard_wall_minutes }}
          TEST_FORCE_AM_FAIL: ${{ inputs.force_am_fail }}
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'

          # [TEST] TEST_FORCE_DATEが設定されていればそれを営業日判定に使う。
          # 本番ファイル(ai-stock-scan.yml)にはこの上書きロジックは存在しない。
          TRADING_DAY=1
          if ! python - <<'PY'
          import os
          import sys
          from datetime import datetime, date
          from zoneinfo import ZoneInfo
          try:
              from common import is_tse_trading_day
              force = os.environ.get('TEST_FORCE_DATE', '').strip()
              today = date.fromisoformat(force) if force else datetime.now(ZoneInfo('Asia/Tokyo')).date()
              trading = is_tse_trading_day(today)
              print(f'ℹ️ [TEST] 営業日判定: {today} (force_date={force!r}) trading_day={trading}')
          except Exception as e:
              print(f'⚠️ 営業日判定に失敗、安全側(取引日扱い)で継続します: {e}')
              trading = True
          sys.exit(0 if trading else 1)
          PY
          then
            TRADING_DAY=0
          fi

          if [ "$TRADING_DAY" -eq 0 ]; then
            echo "🏖 [TEST] 本日は東証休場日のためセッションをスキップします(git commit/pushも行いません)"
            exit 0
          fi

          if [ "${TEST_FORCE_AM_FAIL:-false}" = "true" ]; then
            echo "💥 [TEST] force_am_fail=trueのため、営業日ガード通過直後にAMセッションを失敗させます"
            exit 1
          fi

          case "$SESSION" in
            AM) SESSION_END_MIN=755 ;;
            PM) SESSION_END_MIN=935 ;;
            *) echo "❌ SESSION未設定"; exit 1 ;;
          esac

          HARD_WALL_MIN=${TEST_HARD_WALL_MIN:-320}
          START_EPOCH=$(date +%s)
          HARD_WALL_EPOCH=$((START_EPOCH + HARD_WALL_MIN*60))
          FAIL_STREAK=0
          ITER=0
          STOP=0
          trap 'echo "🛑 中断シグナル受信、次のチェックポイントでループを終了します"; STOP=1' INT TERM

          while true; do
            if [ "$STOP" -eq 1 ]; then
              echo "🛑 中断要求によりセッションを終了します"
              break
            fi

            ITER=$((ITER+1))
            NOW_EPOCH=$(date +%s)
            CUR_MIN=$(TZ=Asia/Tokyo date +%H:%M | awk -F: '{print $1*60+$2}')

            if [ "$CUR_MIN" -ge "$SESSION_END_MIN" ]; then
              echo "🏁 セッション終了時刻に到達 (JST分=$CUR_MIN >= $SESSION_END_MIN)"
              break
            fi
            if [ "$NOW_EPOCH" -ge "$HARD_WALL_EPOCH" ]; then
              echo "🏁 [TEST] ${HARD_WALL_MIN}分の上限に到達、セッションを終了します"
              break
            fi

            echo "▶ tick $(date -u +%H:%M:%S)Z JST分=$CUR_MIN iteration=$ITER SESSION=$SESSION"

            if (
            set -e
            rm -f scan_candidates_cache.json

            python intraday_scheduler.py --phase auto

            python - <<'PY'
          from datetime import datetime
          from zoneinfo import ZoneInfo
          import subprocess
          now=datetime.now(ZoneInfo('Asia/Tokyo')); minutes=now.hour*60+now.minute
          if minutes < 570:
              print(f'▶ 09:30前: 分析・TOP10再評価のみ実行 {now:%H:%M JST}')
              subprocess.run(['python','paper_fast_entrypoint.py','--analysis-only'],check=True)
          else:
              print(f'⏭ 09:30以降: pre-09:30分析フェーズをスキップ {now:%H:%M JST}')
          PY

            python - <<'PY'
          from datetime import datetime
          from zoneinfo import ZoneInfo
          now=datetime.now(ZoneInfo('Asia/Tokyo')); minutes=now.hour*60+now.minute
          if minutes < 570:
              print(f'⏸ 09:30前のためペーパートレード開始を待機: {now:%H:%M JST}')
              raise SystemExit(0)
          if minutes >= 930:
              print(f'⏸ 市場終了後のためペーパートレードを実行しません: {now:%H:%M JST}')
              raise SystemExit(0)
          print(f'▶ 09:30以降: FAST Profit Loop TOP10を実行 {now:%H:%M JST}')
          PY
            python paper_fast_entrypoint.py

            python - <<'PY'
          from datetime import datetime
          from zoneinfo import ZoneInfo
          now=datetime.now(ZoneInfo('Asia/Tokyo'))
          if now.hour*60+now.minute < 570:
              print(f'⏸ 09:30前なのでstate検証をスキップ: {now:%H:%M JST}')
          else:
              import json
              from pathlib import Path
              p=Path('profit_top10_paper_state.json')
              if not p.exists(): raise SystemExit('❌ 09:30以降なのにpaper stateがありません')
              s=json.loads(p.read_text(encoding='utf-8')); positions=s.get('positions',[])
              if len(positions)>10: raise SystemExit(f'❌ TOP10超過: {len(positions)}')
              if int(s.get('trades_today',0))>30: raise SystemExit(f'❌ 1日30回超過: {s.get("trades_today")}')
              print(f'✅ 仮想資産: {float(s.get("capital",0)):,.0f}円')
              print(f'✅ 最大DD: {float(s.get("max_dd",0)):.2f}%')
              print(f'✅ 保有ポジション: {len(positions)}/10')
              print(f'✅ 本日新規: {int(s.get("trades_today",0))}/30')
          PY

            python profit_top10_monthly_performance.py

            echo "🧪 [TEST] git add/commit/pushはスキップします(本番stateを保護するため)"
            ); then
              ITER_EXIT=0
            else
              ITER_EXIT=$?
            fi

            if [ "$ITER_EXIT" -eq 0 ]; then
              FAIL_STREAK=0
            else
              FAIL_STREAK=$((FAIL_STREAK+1))
              echo "⚠️ イテレーション失敗 (exit=$ITER_EXIT), 連続失敗=$FAIL_STREAK"
              if [ "$FAIL_STREAK" -ge 6 ]; then
                echo "❌ 連続6回(30分)失敗のためセッションを中断します"
                exit 1
              fi
            fi

            if [ "$STOP" -eq 1 ]; then
              echo "🛑 中断要求によりセッションを終了します"
              break
            fi

            NOW_SEC=$(date +%s)
            REMAINDER=$((NOW_SEC % 300))
            SLEEP_SEC=$((300 - REMAINDER))
            if [ "$SLEEP_SEC" -ge 300 ]; then SLEEP_SEC=300; fi
            echo "💤 次のtickまで${SLEEP_SEC}秒sleep"
            sleep "$SLEEP_SEC" &
            SLEEP_PID=$!
            wait "$SLEEP_PID" || true
          done

  session_pm:
    needs: [session_am]
    if: ${{ !cancelled() }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SESSION: PM
    steps:
      - name: Checkout latest main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas numpy requests scikit-learn joblib jpholiday

      - name: Apply Nikkei 225 stock universe
        run: |
          python apply_expanded_universe.py
          python - <<'PY'
          import ast
          from pathlib import Path
          tree=ast.parse(Path('daily_directional_top1.py').read_text(encoding='utf-8'))
          vals=[]
          for node in tree.body:
              if isinstance(node,ast.Assign):
                  for target in node.targets:
                      if isinstance(target,ast.Name) and target.id=='TICKERS': vals=ast.literal_eval(node.value)
          print(f'✅ Runtime universe = {len(vals)}銘柄')
          if len(vals) != 225: raise SystemExit('❌ 日経225ユニバースが225銘柄ではありません')
          PY

      - name: Syntax check
        run: |
          python -m py_compile nikkei225_universe.py apply_expanded_universe.py intraday_scheduler.py run_profit_loop.py paper_fast_entrypoint.py paper_profit_entrypoint.py paper_risk_policy.py daily_model_retrain.py walk_forward.py adversarial_strategy_validator.py adversarial_oos_diagnostic.py multi_oos_profit_validator.py build_verified_profit_policy.py daily_directional_top1.py profit_top10_paper.py profit_top10_monthly_performance.py intraday_directional_top1_cycle.py

      - name: Verify Nikkei 225 tickers
        run: |
          python - <<'PY'
          import ast
          from pathlib import Path
          files=['daily_directional_top1.py','walk_forward.py','stock_scan.py']
          counts=[]
          for f in files:
              tree=ast.parse(Path(f).read_text(encoding='utf-8')); vals=[]
              for node in tree.body:
                  if isinstance(node,ast.Assign):
                      for target in node.targets:
                          if isinstance(target,ast.Name) and target.id=='TICKERS': vals=ast.literal_eval(node.value)
              if len(vals)!=225 or len(vals)!=len(set(vals)): raise SystemExit(f'❌ {f}: {len(vals)}銘柄/重複あり')
              counts.append(len(vals)); print(f'✅ {f}: {len(vals)}銘柄・重複なし')
          if len(set(counts))!=1: raise SystemExit(f'❌ 3ファイルの銘柄数不一致: {counts}')
          PY

      - name: Verify paper policy mode
        run: |
          test -s strategy_policy.json
          python - <<'PY'
          import json
          p=json.load(open('strategy_policy.json',encoding='utf-8'))
          status=str(p.get('status','')).upper()
          if status not in ('APPROVED','PENDING'):
              raise SystemExit(f'❌ 不正なpolicy status: {status}')
          print(f'✅ paper policy mode = {status}')
          if status == 'PENDING': print('⚠️ PENDINGでも紙取引は継続。実注文には使用しません。')
          PY

      - name: PM session loop [TEST] (休場日ガード/キャンセル応答性の検証用)
        env:
          AI_POLICY_SIGNING_SECRET: ${{ secrets.AI_POLICY_SIGNING_SECRET }}
          AI_INITIAL_CAPITAL: '1000000'
          INTRADAY_FEE_RATE: '0.00055'
          AI_DAILY_STOP_LOSS: '0.015'
          AI_MAX_DRAWDOWN: '0.30'
          AI_MAX_POSITIONS: '10'
          MAX_TRADES_PER_DAY: '30'
          MAX_TRADES_PER_TICKER_PER_DAY: '10'
          SAME_TICKER_COOLDOWN_MINUTES: '30'
          PAPER_TRADE_MODE: '1'
          TEST_FORCE_DATE: ${{ inputs.force_date }}
          TEST_HARD_WALL_MIN: ${{ inputs.hard_wall_minutes }}
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'

          TRADING_DAY=1
          if ! python - <<'PY'
          import os
          import sys
          from datetime import datetime, date
          from zoneinfo import ZoneInfo
          try:
              from common import is_tse_trading_day
              force = os.environ.get('TEST_FORCE_DATE', '').strip()
              today = date.fromisoformat(force) if force else datetime.now(ZoneInfo('Asia/Tokyo')).date()
              trading = is_tse_trading_day(today)
              print(f'ℹ️ [TEST] 営業日判定: {today} (force_date={force!r}) trading_day={trading}')
          except Exception as e:
              print(f'⚠️ 営業日判定に失敗、安全側(取引日扱い)で継続します: {e}')
              trading = True
          sys.exit(0 if trading else 1)
          PY
          then
            TRADING_DAY=0
          fi

          if [ "$TRADING_DAY" -eq 0 ]; then
            echo "🏖 [TEST] 本日は東証休場日のためセッションをスキップします(git commit/pushも行いません)"
            exit 0
          fi

          case "$SESSION" in
            AM) SESSION_END_MIN=755 ;;
            PM) SESSION_END_MIN=935 ;;
            *) echo "❌ SESSION未設定"; exit 1 ;;
          esac

          HARD_WALL_MIN=${TEST_HARD_WALL_MIN:-320}
          START_EPOCH=$(date +%s)
          HARD_WALL_EPOCH=$((START_EPOCH + HARD_WALL_MIN*60))
          FAIL_STREAK=0
          ITER=0
          STOP=0
          trap 'echo "🛑 中断シグナル受信、次のチェックポイントでループを終了します"; STOP=1' INT TERM

          while true; do
            if [ "$STOP" -eq 1 ]; then
              echo "🛑 中断要求によりセッションを終了します"
              break
            fi

            ITER=$((ITER+1))
            NOW_EPOCH=$(date +%s)
            CUR_MIN=$(TZ=Asia/Tokyo date +%H:%M | awk -F: '{print $1*60+$2}')

            if [ "$CUR_MIN" -ge "$SESSION_END_MIN" ]; then
              echo "🏁 セッション終了時刻に到達 (JST分=$CUR_MIN >= $SESSION_END_MIN)"
              break
            fi
            if [ "$NOW_EPOCH" -ge "$HARD_WALL_EPOCH" ]; then
              echo "🏁 [TEST] ${HARD_WALL_MIN}分の上限に到達、セッションを終了します"
              break
            fi

            echo "▶ tick $(date -u +%H:%M:%S)Z JST分=$CUR_MIN iteration=$ITER SESSION=$SESSION"

            if (
            set -e
            rm -f scan_candidates_cache.json

            python intraday_scheduler.py --phase auto

            python - <<'PY'
          from datetime import datetime
          from zoneinfo import ZoneInfo
          import subprocess
          now=datetime.now(ZoneInfo('Asia/Tokyo')); minutes=now.hour*60+now.minute
          if minutes < 570:
              print(f'▶ 09:30前: 分析・TOP10再評価のみ実行 {now:%H:%M JST}')
              subprocess.run(['python','paper_fast_entrypoint.py','--analysis-only'],check=True)
          else:
              print(f'⏭ 09:30以降: pre-09:30分析フェーズをスキップ {now:%H:%M JST}')
          PY

            python - <<'PY'
          from datetime import datetime
          from zoneinfo import ZoneInfo
          now=datetime.now(ZoneInfo('Asia/Tokyo')); minutes=now.hour*60+now.minute
          if minutes < 570:
              print(f'⏸ 09:30前のためペーパートレード開始を待機: {now:%H:%M JST}')
              raise SystemExit(0)
          if minutes >= 930:
              print(f'⏸ 市場終了後のためペーパートレードを実行しません: {now:%H:%M JST}')
              raise SystemExit(0)
          print(f'▶ 09:30以降: FAST Profit Loop TOP10を実行 {now:%H:%M JST}')
          PY
            python paper_fast_entrypoint.py

            python - <<'PY'
          from datetime import datetime
          from zoneinfo import ZoneInfo
          now=datetime.now(ZoneInfo('Asia/Tokyo'))
          if now.hour*60+now.minute < 570:
              print(f'⏸ 09:30前なのでstate検証をスキップ: {now:%H:%M JST}')
          else:
              import json
              from pathlib import Path
              p=Path('profit_top10_paper_state.json')
              if not p.exists(): raise SystemExit('❌ 09:30以降なのにpaper stateがありません')
              s=json.loads(p.read_text(encoding='utf-8')); positions=s.get('positions',[])
              if len(positions)>10: raise SystemExit(f'❌ TOP10超過: {len(positions)}')
              if int(s.get('trades_today',0))>30: raise SystemExit(f'❌ 1日30回超過: {s.get("trades_today")}')
              print(f'✅ 仮想資産: {float(s.get("capital",0)):,.0f}円')
              print(f'✅ 最大DD: {float(s.get("max_dd",0)):.2f}%')
              print(f'✅ 保有ポジション: {len(positions)}/10')
              print(f'✅ 本日新規: {int(s.get("trades_today",0))}/30')
          PY

            python profit_top10_monthly_performance.py

            echo "🧪 [TEST] git add/commit/pushはスキップします(本番stateを保護するため)"
            ); then
              ITER_EXIT=0
            else
              ITER_EXIT=$?
            fi

            if [ "$ITER_EXIT" -eq 0 ]; then
              FAIL_STREAK=0
            else
              FAIL_STREAK=$((FAIL_STREAK+1))
              echo "⚠️ イテレーション失敗 (exit=$ITER_EXIT), 連続失敗=$FAIL_STREAK"
              if [ "$FAIL_STREAK" -ge 6 ]; then
                echo "❌ 連続6回(30分)失敗のためセッションを中断します"
                exit 1
              fi
            fi

            if [ "$STOP" -eq 1 ]; then
              echo "🛑 中断要求によりセッションを終了します"
              break
            fi

            NOW_SEC=$(date +%s)
            REMAINDER=$((NOW_SEC % 300))
            SLEEP_SEC=$((300 - REMAINDER))
            if [ "$SLEEP_SEC" -ge 300 ]; then SLEEP_SEC=300; fi
            echo "💤 次のtickまで${SLEEP_SEC}秒sleep"
            sleep "$SLEEP_SEC" &
            SLEEP_PID=$!
            wait "$SLEEP_PID" || true
          done
