name: LEGACY DISABLED - TSE 場中監視

on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'この旧場中監視ワークフローは停止済みです。統合版AI Stock Scan - Directional TOP1 5分足 Paper Tradeを使用してください。'
