name: LEGACY DISABLED - TOP3 5分足 自動エントリー

on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'この旧TOP3ワークフローは停止済みです。統合版AI Stock Scan - Directional TOP1 5分足 Paper Tradeを使用してください。'
